[package]
name = "drivers"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
defmt = "0.3"
embedded-hal-async = "1.0.0"

[dev-dependencies]
mockall = "0.13.0"
futures = "0.3"
rstest = "0.22.0"
approx = "0.5.1"
